[uwsgi]
project = covid-research-backend
uid = ubuntu
base = /home/%(uid)/%(project)

chdir = %(base)
home = %(base)/venv
module = %(project).wsgi:application

master = true
processes = 5

socket = %(base)/%(project).sock
chown-socket = %(uid):www-data
chmod-socket = 666
vacuum = true
